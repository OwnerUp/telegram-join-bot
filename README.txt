Telegram Join Request Auto Approve Bot

Setup:
1. Install Python
2. Install requirements:
   pip install -r requirements.txt

3. Add your token in Railway/Render environment variable:
   TOKEN=your_bot_token

4. Run:
   python Bot.py

Important:
- Bot must be admin
- Enable "Approve Join Requests"
